import CostMainProducts from '../CostMainProducts/CostMainProducts';

export default function CostBundleProducts({ currentInfoProduct }) {
  return (
    <div>
      <CostMainProducts currentInfoProduct={currentInfoProduct} />
    </div>
  );
}
